sandbox_mode = "read-only"

[features]
shell_tool = false
unified_exec = false

[mcp_servers.rhino]
tool_timeout_sec = 3600
# "approve" pre-approves the tools; "auto" defers to the policy `codex exec` pins to "never", blocking every call.
default_tools_approval_mode = "approve"
